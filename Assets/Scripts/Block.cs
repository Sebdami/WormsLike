using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Block {
    int blockId = 0;

    public Block(int _blockId)
    {
        blockId = _blockId;
    }
}
